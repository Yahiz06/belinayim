<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Belinay ??</title>

  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: "Arial", sans-serif;
      background: linear-gradient(135deg, #1f3b2c, #5a3d2b);
      color: white;
      text-align: center;
      overflow-x: hidden;
    }

    .container {
      padding: 60px 20px;
      max-width: 600px;
      margin: auto;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 10px;
      color: #b6ffb6;
      text-shadow: 0px 0px 10px rgba(0,0,0,0.5);
    }

    p {
      font-size: 1.1rem;
      line-height: 1.6;
      opacity: 0.95;
    }

    .card {
      background: rgba(0,0,0,0.35);
      border-radius: 20px;
      padding: 25px;
      margin-top: 30px;
      box-shadow: 0 0 25px rgba(0,0,0,0.4);
      border: 1px solid rgba(255,255,255,0.1);
    }

    .cat {
      font-size: 80px;
      margin: 20px 0;
      animation: float 2.5s ease-in-out infinite;
    }

    @keyframes float {
      0% { transform: translateY(0px); }
      50% { transform: translateY(-12px); }
      100% { transform: translateY(0px); }
    }

    button {
      margin-top: 20px;
      padding: 14px 25px;
      border: none;
      border-radius: 30px;
      font-size: 1.05rem;
      cursor: pointer;
      background: #b6ffb6;
      color: #1f3b2c;
      font-weight: bold;
      transition: 0.3s;
      box-shadow: 0 0 15px rgba(182,255,182,0.4);
    }

    button:hover {
      transform: scale(1.05);
      background: #8eff8e;
    }

    .hidden-message {
      display: none;
      margin-top: 25px;
      font-size: 1.2rem;
      font-weight: bold;
      color: #ffe9c7;
      text-shadow: 0px 0px 10px rgba(0,0,0,0.6);
    }

    .heart {
      position: fixed;
      top: -10px;
      font-size: 22px;
      animation: fall 5s linear infinite;
      opacity: 0.8;
    }

    @keyframes fall {
      0% { transform: translateY(-20px); }
      100% { transform: translateY(110vh); }
    }

    .footer {
      margin-top: 50px;
      font-size: 0.9rem;
      opacity: 0.7;
    }

    audio {
      margin-top: 20px;
      width: 100%;
      border-radius: 20px;
    }
  </style>
</head>

<body>

  <div class="container">
    <h1>Belinay ??</h1>
    <p>
      Bu sayfa sadece senin için.  
      Çünkü bazen bazý insanlar, insanýn hayatýna sessizce girip her þeyi güzelleþtiriyor.
    </p>

    <div class="cat">??</div>

    <div class="card">
      <p>
        Yeþilin huzuru gibi…  
        Kahverenginin sýcaklýðý gibi…  
        Sen de öylesin Belinay.
      </p>

      <p>
        Bunu okurken yüzünde küçücük bir gülümseme oluþursa,
        bil ki benim için yeter.
      </p>

      <button onclick="showMessage()">Bana týkla ??</button>

      <div class="hidden-message" id="msg">
        Belinay… seni düþündüðümde içim garip bir þekilde sakinleþiyor.  
        Ýyi ki varsýn. ??
      </div>

      <audio controls>
        <source src="https://www.mboxdrive.com/Duman%20-%20Bal.mp3" type="audio/mpeg">
        Tarayýcýn müziði desteklemiyor.
      </audio>

      <p style="font-size: 0.85rem; opacity: 0.75; margin-top: 10px;">
        ?? Duman - Bal (çalmazsa sorun deðil, sayfa yine efsane)
      </p>
    </div>

    <div class="footer">
      <p>? Bu sayfa birinin kalbinden geldi.</p>
    </div>
  </div>

  <script>
    function showMessage() {
      document.getElementById("msg").style.display = "block";
      createHearts();
    }

    function createHearts() {
      for (let i = 0; i < 25; i++) {
        let heart = document.createElement("div");
        heart.classList.add("heart");
        heart.innerHTML = "??";
        heart.style.left = Math.random() * 100 + "vw";
        heart.style.animationDuration = (Math.random() * 2 + 3) + "s";
        document.body.appendChild(heart);

        setTimeout(() => {
          heart.remove();
        }, 6000);
      }
    }
  </script>

</body>
</html>
